{
  "name": "music-notes-trainer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest --pool=threads"
  },
  "devDependencies": {
    "vitest": "^4.0.0"
  }
}
